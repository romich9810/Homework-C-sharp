using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Cryptography.X509Certificates;
using System.Text;
using System.Threading.Tasks;

namespace CSLight
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int firstMemberOfRow = 5;
            int memberOfRow;
            int stepOfIteration = 0;
            int stepOfRow = 7;

            for (memberOfRow = 0; memberOfRow < 96 ; stepOfIteration++)
            {
                memberOfRow = stepOfRow * stepOfIteration + firstMemberOfRow;
                Console.WriteLine(memberOfRow);
            }
        }
    }
}
