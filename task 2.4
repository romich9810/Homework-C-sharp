using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Cryptography.X509Certificates;
using System.Text;
using System.Threading.Tasks;

namespace CSLight
{
    internal class Program
    {
        static void Main(string[] args)
        {
            Random random = new Random();
            int minimalNumber = 1;
            int maximalNumber = 100;
            int number = random.Next(minimalNumber, maximalNumber + 1);
            int multipleFirstNumber = 3;
            int multipleSecondNumber = 5;
            int sumOfNumbers = 0;

            Console.WriteLine($"Великий рандом решил, что это число {number}");

            for (int i = 1; i <= number; i++)
            {
                Console.WriteLine(i);

                if ((i % multipleFirstNumber == 0) || (i % multipleSecondNumber == 0))
                {
                    sumOfNumbers += i;

                    if (i % multipleFirstNumber == 0)
                    {
                        Console.Write("\bДелится на 3\n");
                    }
                    if (i % multipleSecondNumber == 0)
                    {
                        Console.Write("\bДелится на 5\n");
                    }
                }
            }

            Console.WriteLine($"Сумма всех положительных чисел, кратных 3 и 5 = {sumOfNumbers}");
        }
    }
}
